# Shipped binaries — never apply line-ending or encoding conversion (would corrupt them).
*.wasm  binary
*.onnx  binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.exe   binary

# Mac launchers must keep Unix line endings or macOS will not run them.
*.command text eol=lf
